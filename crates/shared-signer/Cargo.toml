[package]
name = "shared-signer"
version = "2.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Shared no_std wire types and QR framing for KasSigner and KasSee"

[lib]
path = "src/lib.rs"

[dependencies]
sha2 = { version = "0.10", default-features = false }
